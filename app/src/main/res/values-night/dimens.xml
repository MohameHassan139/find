<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark design: the header sits flat on #2C2C2E (separated by a 0.5dp line)
         and category cards use a white glow instead of a black elevation shadow. -->
    <dimen name="header_elevation">0dp</dimen>
    <dimen name="category_card_elevation">0dp</dimen>
    <!-- Dark design: the glass nav pill has no drop shadow -->
    <dimen name="bottom_nav_elevation">0dp</dimen>
    <!-- Dark design: 3dp divider under each yellow tab row, active indicator overlays it -->
    <dimen name="tab_row_strip">3dp</dimen>
    <dimen name="tab_row_gap">3dp</dimen>
    <dimen name="ad_details_card_elevation">0dp</dimen>
</resources>
