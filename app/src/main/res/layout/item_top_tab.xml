<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:paddingHorizontal="14dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackground">

    <!-- Mirrors the underline height so the label stays exactly centered in the
         yellow bar whether the tab is selected or not -->
    <View
        android:layout_width="0dp"
        android:layout_height="3dp" />

    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.example.myapplication.widgets.StrokeTextView
        android:id="@+id/tvLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:includeFontPadding="false"
        android:text="@string/str_label"
        android:textColor="@color/tab_label_inactive"
        android:textSize="16sp" />

    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Highlighter sits ON the yellow bar (above the row's bottom strip) -->
    <View
        android:id="@+id/underline"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_marginBottom="@dimen/tab_row_strip"
        android:background="@android:color/transparent" />

</LinearLayout>
